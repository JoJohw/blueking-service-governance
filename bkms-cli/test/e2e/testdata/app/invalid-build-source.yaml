# 错误用例：buildConfig.sourceType 值非法
name: my-app
type: trpc

buildConfig:
  sourceType: docker
  imageBuildConfig:
    name: example.com/myns/my-app

appModelSpec:
  trpcSpec:
    language: go
    fileName: trpc_go.yaml
    filePath: /app/conf
