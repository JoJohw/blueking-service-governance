# 错误用例：helmSpec.helmSource.repoType 值非法
name: my-helm-app
type: helm

buildConfig:
  sourceType: imageRegistry
  imageBuildConfig:
    name: example.com/myns/my-helm-app

helmSpec:
  helmSource:
    repoType: SVNRepo
    helmRepoConfig:
      repoURL: https://charts.example.com
      chartName: my-chart
