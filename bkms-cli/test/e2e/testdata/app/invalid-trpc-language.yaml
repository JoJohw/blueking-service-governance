# 错误用例：appModelSpec.trpcSpec.language 值非法
name: my-app
type: trpc

buildConfig:
  sourceType: imageRegistry
  imageBuildConfig:
    name: example.com/myns/my-app

appModelSpec:
  trpcSpec:
    language: java
    fileName: trpc_go.yaml
    filePath: /app/conf
