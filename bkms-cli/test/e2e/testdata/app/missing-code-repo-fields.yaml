# 错误用例：sourceType=codeRepository 时缺少 repoBuildConfig 必填字段
name: my-app
type: trpc

buildConfig:
  sourceType: codeRepository
  repoBuildConfig: {}
  # 缺少 type, repoAlias, repoURL, defaultBranch

appModelSpec:
  trpcSpec:
    language: go
    fileName: trpc_go.yaml
    filePath: /app/conf
