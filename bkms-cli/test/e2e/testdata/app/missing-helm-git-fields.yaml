# 错误用例：helmSpec.helmSource.repoType=GitRepo 时缺少 gitRepoConfig 必填字段
name: my-git-app
type: helm

buildConfig:
  sourceType: imageRegistry
  imageBuildConfig:
    name: example.com/myns/my-git-app

helmSpec:
  helmSource:
    repoType: GitRepo
    gitRepoConfig: {}