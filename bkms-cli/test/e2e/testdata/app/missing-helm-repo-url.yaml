# 错误用例：helmSpec.helmSource.repoType=HelmRepo 时缺少 helmRepoConfig 必填字段
name: my-helm-app
type: helm

buildConfig:
  sourceType: imageRegistry
  imageBuildConfig:
    name: example.com/myns/my-helm-app

helmSpec:
  helmSource:
    repoType: HelmRepo
    helmRepoConfig: {}