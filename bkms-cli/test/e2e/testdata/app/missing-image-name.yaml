# 错误用例：sourceType=imageRegistry 时缺少 imageBuildConfig.name
name: my-app
type: trpc

buildConfig:
  sourceType: imageRegistry
  imageBuildConfig: {}

appModelSpec:
  trpcSpec:
    language: go
    fileName: trpc_go.yaml
    filePath: /app/conf
