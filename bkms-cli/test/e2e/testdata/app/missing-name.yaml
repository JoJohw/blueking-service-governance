# 错误用例：缺少 name 字段
type: trpc

buildConfig:
  sourceType: imageRegistry
  imageBuildConfig:
    name: example.com/myns/my-app

appModelSpec:
  trpcSpec:
    language: go
    fileName: trpc_go.yaml
    filePath: /app/conf
