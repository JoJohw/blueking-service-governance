# 错误用例：sourceType=pipeline 时缺少 pipelineBuildConfig.pipelineID
name: my-app
type: trpc

buildConfig:
  sourceType: pipeline
  pipelineBuildConfig: {}

appModelSpec:
  trpcSpec:
    language: go
    fileName: trpc_go.yaml
    filePath: /app/conf
