# scripts

## Conventions

- Keep scripts small and practical.
- Script should have necessary comments and docstring.
- Bash scripts should use `#!/usr/bin/env bash` and `set -euo pipefail`.
- Python scripts should prefer PEP-723 inline dependency metadata.
- Update `README.md` whenever a script's behavior or usage changes.
