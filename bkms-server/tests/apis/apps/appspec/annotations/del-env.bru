meta {
  name: del-env
  type: http
  seq: 7
}

delete {
  url: {{serviceURL}}/apps/{{annotationsAppID}}/envs/test/app-spec/annotations
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
