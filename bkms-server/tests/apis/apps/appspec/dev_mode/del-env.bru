meta {
  name: del-env
  type: http
  seq: 6
}

delete {
  url: {{serviceURL}}/apps/{{devModeAppID}}/envs/test/app-spec/dev-mode
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
