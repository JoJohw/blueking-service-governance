meta {
  name: del-env
  type: http
  seq: 8
}

delete {
  url: {{serviceURL}}/apps/{{labelsAppID}}/envs/test/app-spec/labels
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
