meta {
  name: set-default-exec-with-command
  type: http
  seq: 3
}

put {
  url: {{serviceURL}}/apps/{{lifecycleAppID}}/app-spec/default-lifecycle
  body: json
  auth: inherit
}

body:json {
  {
    "appSpecLifecycle": {
      "postStart": {
        "type": "EXEC",
        "exec": {
          "command": ["sh", "-c", "echo started"]
        }
      }
    }
  }
}

assert {
  res.status: eq 200
  res.body: isJson
}

settings {
  encodeUrl: true
  timeout: 0
}
