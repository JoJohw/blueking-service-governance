meta {
  name: env-get-after-del
  type: http
  seq: 11
}

get {
  url: {{serviceURL}}/apps/{{probeAppID}}/envs/{{testEnv}}/app-spec/probe
  auth: inherit
}

assert {
  res.status: eq 404
  res.body: isJson
}

tests {
  test("should return not found after deleting env probe override", () => {
    expect(res.body.error.message).to.contains("probe not found");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
