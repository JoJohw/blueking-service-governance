meta {
  name: delete-env-binding
  type: http
  seq: 7
}

delete {
  url: {{serviceURL}}/apps/{{bscpCfgAppID}}/bscpcfg/envs/{{bscpCfgEnvName}}/binding
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("should delete env binding", () => {
    expect(res.status).to.equal(200);
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
