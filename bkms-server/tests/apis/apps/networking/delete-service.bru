meta {
  name: delete-service
  type: http
  seq: 6
}

delete {
  url: {{serviceURL}}/apps/{{networkingHelmAppID}}/services/{{newServiceName}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
