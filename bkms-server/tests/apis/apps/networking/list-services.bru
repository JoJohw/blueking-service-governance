meta {
  name: list-services
  type: http
  seq: 2
}

get {
  url: {{serviceURL}}/apps/{{networkingHelmAppID}}/services
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
