meta {
  name: delete-trpc-app
  type: http
  seq: 4
}

delete {
  url: {{serviceURL}}/apps/{{testTrpcAppID}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
}

tests {
  test("should delete trpc app successfully", () => {
    expect(res.status).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 10000
}
