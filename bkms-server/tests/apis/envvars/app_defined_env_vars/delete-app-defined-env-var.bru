meta {
  name: delete-app-defined-env-var
  type: http
  seq: 6
}

delete {
  url: {{serviceURL}}/apps/{{appDefinedEnvVarsAppID}}/env-vars/{{appDefinedEnvVarKey}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
