# S2 测试用例独立评审记录

> 定位：S2（`test/scenarios/deploy-management.test.ts`）独立质量评审，与实施记录 `../pilots/TEST_PILOT_S2.md` 成对。方法论：QA Skill-Suite `qa-testcase-review`（六维 + 门禁）。

## 1. 评审元信息

| 项 | 内容 |
|---|---|
| 评审对象 | `test/scenarios/deploy-management.test.ts`（2 个测试） |
| 被测源码 | `src/pages/application/detail/deploy/deploy.vue`（`canManageFeatureEnvs`） |
| 评审基准 | S2 场景卡（`../guides/TEST_SCENARIOS_ROUTES.md`）；`../guides/TEST_GUIDELINE.md` §3 |

## 2. 评审时间线

| 轮次 | 得分 | 结论 | 关键发现 |
|---|---|---|---|
| 初评（含自整改） | 80/100 | 通过 | 需 vxe 垫片 + 放宽超时；仍有真实请求噪音未消除 |

## 3. 六维评分

| 维度 | 得分 | 满分 | 证据摘要 |
|---|---:|---:|---|
| 完整性 | 18 | 30 | 仅覆盖入口权限分发两条；台账 V=4 中的「提交成功/失败/加载中」未覆盖，子页（实例列表/部署历史/资源拓扑）全部 stub |
| 准确性 | 22 | 25 | 断言与源码核实一致（`isAppModelAppType`）；子页 stub -2；真实请求未屏蔽 -1 |
| 有效性 | 14 | 15 | 变异 2/2 捕获 |
| 可执行性 | 7 | 10 | 约 9s/次（页面重）；需 pinia + vxe 垫片 + 20s 超时 |
| 规范性 | 9 | 10 | 标题模板、cleanup 符合；stub 与超时均有注释 -1 |
| 可维护性 | 10 | 10 | 工厂函数清晰，子页 stub 集中 |

## 4. 问题闭环总表

| 级别 | 内容 | 状态 |
|---|---|---|
| Medium | 缺 vxe 垫片与超时导致 0/2 | ✅ 修复 |
| **Medium** | 4 个接口发真实请求（未走已 mock 的 api 模块） | ⏸ 登记 backlog（P2），需定位导入路径补 mock |

## 5. 运行实证

`vitest run test/scenarios/deploy-management.test.ts`：**2 passed (2)**，约 9s/次。

## 6. 有效性实证（变异测试）

| # | 注入的业务回归 | 测试结果 | 命中用例 |
|---|---|---|---|
| 1 | 非模型类也展示特性环境入口（`v-if="true"`） | **1 failed** \| 1 passed | 非模型类不应展示入口 |
| 2 | 模型类不再展示入口（`v-if="false"`） | **1 failed** \| 1 passed | 模型类应展示入口 |

**结论**：2/2 变异被捕获。

## 7. 推广结论

**通过（80/100，无阻塞，但覆盖偏薄）。** 本场景实际只覆盖了部署页的入口权限分发，台账定义的「提交成功/失败/加载中」主路径尚未覆盖——**S2 应视为「部分完成」，建议后续专项补齐部署提交流程**（precheck → 提交 → 结果反馈）。

## 8. 遗留项

**真缺口**：

- **P1**：部署提交主路径（选择环境 → 提交 → 成功/失败/加载中），台账 V=4 的核心。
- **P2**：4 个接口的真实请求需补 mock。
- **P2**：三个 Tab 子页（部署总览、实例列表、资源拓扑）的交互。

**N/A**：

- **N/A-B（stub 代价）**：子页内部表格/图表行为。

**其他**：PILOT_S2 §4 文档性验收待人工执行。
