# S9 测试用例独立评审记录

> 定位：S9（`test/scenarios/env-management.test.ts`）独立质量评审，与实施记录 `../pilots/TEST_PILOT_S9.md` 成对。方法论：QA Skill-Suite `qa-testcase-review`（六维 + 门禁）。

## 1. 评审元信息

| 项 | 内容 |
|---|---|
| 评审对象 | `test/scenarios/env-management.test.ts`（7 个测试） |
| 被测源码 | `src/pages/env/env.vue`（`handleGetEnvList`、`handleDeleteEnv`、`confirmDeleteEnv`）、`delete-env-dialog.vue` |
| 评审基准 | S9 场景卡（`../guides/TEST_SCENARIOS_ROUTES.md`）；`../guides/TEST_GUIDELINE.md` §3 |

## 2. 评审时间线

| 轮次 | 得分 | 结论 | 关键发现 |
|---|---|---|---|
| 初评（含自整改） | 86/100 | 通过 | 表格方案由「真实 vxe + 垫片」改为「stub + 垫片」；修正多列同值导致的查询歧义 |
| 补缺口后复评 | 90/100 | 通过 | 按指南条款 7（P0/P1 正反+边界）与黄金法则例外（破坏性操作）补 4 条：名称不一致禁用、删除成功、删除失败、已部署应用告警 |

## 3. 六维评分

| 维度 | 得分 | 满分 | 证据摘要 |
|---|---:|---:|---|
| 完整性 | 27 | 30 | 列表有数据/空列表 + 删除全链路（打开/边界拦截/成功/失败/已部署告警）覆盖；剩余：新建环境弹窗（P2）、搜索与排序（P3，需先更新台账 V） |
| 准确性 | 23 | 25 | 断言与源码核实一致（单参 `deleteEnv`、`getEnv` 决定弹窗与否、失败静默）；表格为 stub -1；删除成功以「列表刷新」替代「弹窗消失」（jsdom 过渡节点不移除）-1 |
| 有效性 | 15 | 15 | 变异 4/4 捕获，每个变异只打挂对应用例 |
| 可执行性 | 9 | 10 | 连跑 3 次全绿（约 7s/次）；stub 与垫片并用，环境搭建成本 -1 |
| 规范性 | 10 | 10 | 标题模板、cleanup、stub 注释说明被替换的是组件库职责；业务约定（失败静默）按现状断言 |
| 可维护性 | 9 | 10 | `openDeleteDialog` 辅助收敛样板；弹窗按钮用「最后一个同名按钮」定位，可读性一般 -1 |

## 4. 问题闭环总表

| 级别 | 内容 | 状态 |
|---|---|---|
| **Medium** | vxe 纯字段单元格在 jsdom 下不产出 DOM | ✅ stub 表格（方案 A），模板固化进 skill |
| Medium | 多列同值导致查询歧义 | ✅ mock 数据字段差异化 |
| Low | 删除成功后弹窗 DOM 未移除（过渡动画） | ✅ 改用「列表重新拉取」断言并注释原因 |
| Low | 删除接口为单参调用，初版断言写成两参 | ✅ 修正 |

## 5. 运行实证

`vitest run test/scenarios/env-management.test.ts`：**7 passed (7)**，**连跑 3 次稳定**（6.7~7.0s/次），满足指南 §4.2。

## 6. 有效性实证（变异测试）

| # | 注入的业务回归 | 测试结果 | 命中用例 |
|---|---|---|---|
| 1 | 丢弃接口返回的环境数据（`return []`） | **2 failed** \| 5 passed | 展示环境名称、点击删除打开确认 |
| 2 | 点击删除不再弹出确认 | **1 failed** \| 6 passed | 点击删除应打开确认弹窗 |
| 3 | 删除成功后不再刷新列表 | **1 failed** \| 6 passed | 删除成功应刷新列表 |
| 4 | 已部署应用不再拦截删除（`if (false)`） | **1 failed** \| 6 passed | 已部署应用时不应直接弹删除确认 |

**结论**：4/4 变异被捕获，stub 方案下用例仍具备真实回归检出能力。

## 7. 推广结论

**通过（90/100，无阻塞）。** 本场景确立了列表类场景的标准打法（stub 表格 + 垫片 + 连跑验收），并验证了「破坏性操作链路」值得按指南黄金法则例外优先覆盖。

## 8. 遗留项（按 N/A 判定标准分类，标准见 skill `references/conventions.md`）

**真缺口（后续应补）**：

- **P2**：新建环境弹窗（CreateEnv）与创建流程 —— **不判 N/A**：同类弹窗（S6 的 bkui-vue Dialog）已可测，CreateEnv 失败更可能是内部依赖未 mock 全，需继续排查。
- **P3**：搜索筛选（SearchSelect 触发重新请求）、排序（升降序改变行顺序）—— 均在表格外或可经 stub 行序断言；按指南条款 2 需先更新台账 V 值再写。

**N/A（不适用，不重复立项）**：

- **N/A-A（组件库/环境不可达）**：Dialog 自带 X / 遮罩关闭。
- **N/A-B（stub 方案代价）**：表格分页、列头筛选 —— 分页器与列头由 vxe 渲染，stub 后不存在；方案变更时重评。
- **N/A-C（行为未定）**：删除「已部署应用」的拦截规则本身——当前用例只固化「不直接弹删除确认」这一现象；「是否应该拦截」属业务规则，待业务确认后再补断言。

**其他**：PILOT_S9 §4 文档性验收待人工执行。
