apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "bkms-server.fullname" . }}-migrate-{{ .Release.Revision }}
  labels:
    {{- include "bkms-server.labels" . | nindent 4 }}
    app.kubernetes.io/component: migrate
spec:
  backoffLimit: 5
  template:
    metadata:
      labels:
        {{- include "bkms-server.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: migrate
    spec:
      serviceAccountName: {{ include "bkms-server.serviceAccountName" . }}
      restartPolicy: OnFailure
      containers:
        - name: migrate
          image: {{ include "bkms-server.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - bkms-server
          args:
            - migrate
            - --srvCfg
            - /app/etc/config.yaml
            - up
          resources:
            {{- toYaml .Values.migration.resources | nindent 12 }}
          volumeMounts:
            - name: conf
              mountPath: /app/etc
      volumes:
        - name: conf
          configMap:
            defaultMode: 420
            name: {{ include "bkms-server.configCMName" . }}
      {{- include "bkms-server.imagePullSecrets" . | nindent 6 }}
