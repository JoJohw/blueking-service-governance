apiVersion: v1
kind: Service
metadata:
  name: {{ include "bkms-ui.fullname" . }}
  labels:
    {{- include "bkms-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bkms-ui.selectorLabels" . | nindent 4 }}
